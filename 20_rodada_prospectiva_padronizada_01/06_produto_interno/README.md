# Produto interno — MDN-RPP01

## Status

Bloqueado até G3.

O produto interno deverá apresentar sinais agregados, trilha de evidência, estados de validação, denominadores reconciliados, limitações e pontos que permanecem `nao_determinado`.

## Linguagem obrigatória

- `sinal observado no corpus` em vez de fato populacional;
- `recorrência no conjunto coletado` em vez de prevalência;
- `hipótese de ação` em vez de solução comprovada;
- `sem marcador ou evidência suficiente` em vez de neutro ou ausente;
- `validação interna necessária` para itens não encerrados.

O produto interno não será tratado como autorização de circulação pública.

